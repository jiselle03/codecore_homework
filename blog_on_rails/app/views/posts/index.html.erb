<div class="container">

<h1>All Posts</h1>

<% @posts.each do |post| %>
<div>
    <h2><%= link_to(post.title, post_path(post.id)) %></h2>
    <p><%= post.body %></p>
    <p>Posted <%= time_ago_in_words(post.created_at) %> ago</p>
</div>
<% end %>

</div>